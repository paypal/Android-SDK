package com.paypal.android.ui.theme

import androidx.compose.material.darkColors
import androidx.compose.material.lightColors
import androidx.compose.ui.graphics.Color

private val LightBlue = Color(0xFF009CDE)
private val DarkBlue = Color(0xFF003087)
private val Orange = Color(0xFFFF9600)
private val Pink = Color(0xFFDE0063)
private val Purple = Color(0xFF640487)
private val Green = Color(0xFF00CF92)
private val LightGray = Color(0xFF6C7378)
private val DarkGray = Color(0xFF2C2E2F)

val LightColors = lightColors(
    primary = DarkBlue,
    secondary = LightBlue,
    surface = Color.White,
    background = Green
)

val DarkColors = darkColors(

)